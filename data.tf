# This module is intentionally data-source light. The caller owns cluster,
# subnet, IAM role, and KMS dependencies so live stacks can wire those modules
# explicitly and keep ownership boundaries clear.
